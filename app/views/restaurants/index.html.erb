<h1>The List of all the restaurants</h1>
<%= link_to "Add a new Restaurant", new_restaurant_path %>
<% @restaurants.each do |restaurant| %>
<%= link_to restaurant_path(restaurant) do %>
<div>
  <h3><%= restaurant.name %></h3>
  <h4><%= restaurant.address %></h4>
</div>
<% end %>
<% end %>
